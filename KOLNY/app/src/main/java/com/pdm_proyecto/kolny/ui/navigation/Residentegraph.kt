package com.pdm_proyecto.kolny.ui.navigation

import androidx.navigation.NavGraphBuilder
import androidx.navigation.NavHostController
/*import androidx.navigation.compose.composable
import androidx.navigation.navigation
import com.pdm_proyecto.kolny.ui.screens.residente.**/

fun NavGraphBuilder.residenteGraph(nav: NavHostController) {
    /*navigation(
        startDestination = Route.ResHome.route,
        route = Route.ResidenteRoot.route
    ) {
        composable(Route.ResHome.route) { ResidenteHomeScreen(nav) }
        composable(Route.Perfil.route)  { PerfilScreen(nav) }
    }*/
}